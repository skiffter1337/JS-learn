function divCon(x){
    let stringInt = 0;
    let nonStrInt = 0;
   x.forEach( el => {
       if(typeof el === 'number') {
          nonStrInt += el
       } else {
           stringInt += parseInt(el)
       }

   })
    return nonStrInt - stringInt
}
