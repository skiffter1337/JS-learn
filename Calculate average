function findAverage(array) {
  let average = 0;
  let result = 0;
  if(array == false) {
    return 0
  } else {
  for(let i = 0; i < array.length; i++) {
    average += array[i];
  }
  return average / array.length;
}
}
