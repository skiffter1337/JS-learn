function capitalize(s){
 let evenUp = s.split("").map((el, index) => index % 2 === 0 ? el.toUpperCase() : el).join("")
    let oddUp = s.split("").map((el, index) => index % 2 !== 0 ? el.toUpperCase() : el).join("")
    return [evenUp, oddUp]
};
