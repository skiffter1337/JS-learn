function dontGiveMeFive(start, end) {
    let arr = []
    for (let i = start; i <= end; i++) {
        arr.push("" + i)
    }
    return arr.filter(el => !el.endsWith("5") && !el.startsWith("5")).length
}
